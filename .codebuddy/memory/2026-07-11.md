# 2026-07-11

## 重命名：oah-ai → uct8086-ai
- 将整个项目中的 "oah ai" 名称统一替换为 "uct8086 ai"。
- 映射：`starbucks.oah.ai`/`com.oah.ai`/`oah.ai`/`oah:` → `uct8086.ai`；`oah-ai` → `uct8086-ai`；`.oah`(目录) → `.uct8086`；`OahException` → `Uct8086Exception`（含文件重命名）；`OAH-AI`/`OAH` → `UCT8086-AI`/`UCT8086`。
- `pom.xml` 此前已迁移为 `groupId: uct8086.ai`、`artifactId: uct8086-ai`、`name: UCT8086-AI`。
- `application.yml` 配置前缀为 `uct8086.ai`（`HarnessProperties` 的 `@ConfigurationProperties(prefix="uct8086.ai")` 与之对应），启动类实际名为 `AiApplication`（README 中旧的 `OahAiApplication` 引用已修正）。
- 验证：全仓搜索 `oah|Oah|OAH` 与 `starbucks` 均 0 命中。
