# =========================================
# UCT8086-AI: Open Agent Harness Configuration
# =========================================

server:
  port: 9081

spring:
  application:
    name: uct8086-ai

  # Disable PgVectorStore auto-config — we create our own in PgVectorConfig
  autoconfigure:
    exclude:
      - org.springframework.ai.vectorstore.pgvector.autoconfigure.PgVectorStoreAutoConfiguration

  # Datasource (MySQL)
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/${MYSQL_DATABASE:uct8086_ai}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root.2026}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # SQL initialization (runs schema.sql on startup)
  sql:
    init:
      mode: always

  # Redis
  data:
    redis:
      host: ${REDIS_HOST:0.0.0.0}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 0
          max-wait: -1ms

  # Spring AI Configuration
  ai:
    # Chat: qoder-cli-api bridge (qoder.com international, OpenAI compatible)
    openai:
      api-key: ${QODER_API_KEY:harness-key-2026}
      base-url: http://localhost:3000/v1
      chat:
        options:
          model: qoder/auto
          temperature: 0.7
      embedding:
        enabled: false

    # Embedding: local Ollama
    ollama:
      embedding:
        enabled: true
        options:
          model: bge-m3
      chat:
        enabled: false

# pgvector (PostgreSQL with vector extension)
pgvector:
  datasource:
    url: jdbc:postgresql://${PGVECTOR_HOST:localhost}:5432/${PGVECTOR_DB:postgres}
    username: ${PGVECTOR_USER:postgres}
    password: ${PGVECTOR_PASSWORD:321432}
    driver-class-name: org.postgresql.Driver

# UCT8086-AI Harness Configuration
uct8086:
  ai:
    # Permission mode: DEFAULT, AUTO, PLAN_MODE, READ_ONLY
    permission-mode: DEFAULT

    # Maximum agent loop iterations
    max-turns: 50

    # API retry configuration
    retry-enabled: true
    max-retries: 3
    retry-delay-ms: 1000

    # Parallel tool execution
    parallel-tool-execution: true

    # Context compression (auto-compact)
    context-compression: true
    compression-threshold: 100000

    # Working directory for file operations
    working-directory: ${user.dir}

    # Memory file location (default: <working-directory>/.uct8086/MEMORY.md)
    # memory-file: ${user.dir}/.uct8086/MEMORY.md

    # Model override (optional)
    # model: gpt-4o
    # temperature: 0.7

# Logging
logging:
  file:
    name: logs/uct8086-ai.log
  level:
    uct8086.ai: DEBUG
    org.springframework.ai: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
